new Vue({
    el: '#app',
    data: {
        name: 'X person',
        website: 'https://www.tutorialspoint.com/',
        websiteurl: 'https://www.freeformatter.com/',
        websiteTag: '<a href="https://vuejs.org/">Vue JS</a>'
    },
    methods: {
        sum: function (num1, num2) {
            // var ans;
            return ans = this.name + ' salary is : $' + (num1 + num2);
        }
    }
});